[pytest]
# 测试目录
testpaths = tests

# 减少输出噪音
addopts = 
    -v
    --tb=short
    --strict-markers
    -p no:cacheprovider

# 标记
markers =
    slow: 标记慢速测试
    api: API 测试
    unit: 单元测试

# 过滤警告
filterwarnings =
    ignore::DeprecationWarning
    ignore::PendingDeprecationWarning
