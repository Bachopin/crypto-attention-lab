# 系统运行与测试总结

## ✅ 当前状态

### 服务运行中
- **后端 API**: http://localhost:8000 ✅
- **前端 Web**: http://localhost:3000 ✅
- **API 文档**: http://localhost:8000/docs ✅

### 性能指标
- 健康检查: ~250ms
- 价格数据 API: ~360ms
- Attention 数据 API: ~165ms
- 自动更新状态: ~580ms

### 后台任务
- ✅ 价格更新: 每2分钟自动运行
- ✅ Attention 计算: 价格更新后立即触发
- ✅ 新闻拉取: 每1小时运行
- ✅ 支持代币: ZEC, BTC, ETH, SOL, BNB

---

## 🚀 快速启动

### 一键启动（推荐）
```bash
./scripts/start_services.sh
```

### 分步启动
```bash
# 启动后端
./scripts/start_api.sh

# 启动前端（新终端）
cd web && npm run dev
```

---

## 🔍 监控与管理

### 检查系统状态
```bash
./scripts/check_status.sh
```

### 性能监控
```bash
python3 scripts/monitor_performance.py
```

### 查看日志
```bash
# 后端日志（实时）
tail -f logs/api.log

# 前端日志（实时）
tail -f logs/frontend.log

# 查看最近错误
grep -i error logs/api.log | tail -20
```

### 停止服务
```bash
./scripts/stop_services.sh
```

---

## 🧪 测试端点

### 健康检查
```bash
curl http://localhost:8000/health
```

### 获取代币状态
```bash
curl http://localhost:8000/api/auto-update/status | jq
```

### 获取BTC价格数据
```bash
curl "http://localhost:8000/api/price?symbol=BTC&granularity=1d&start=2025-11-25&end=2025-11-29" | jq
```

### 获取BTC Attention数据
```bash
curl "http://localhost:8000/api/attention?symbol=BTC&start=2025-11-25&end=2025-11-29" | jq
```

---

## ✅ 已完成的优化

1. **依赖修复**
   - ✅ 安装 feedparser 模块
   - ✅ 更新 requirements.txt

2. **架构优化**
   - ✅ 价格更新后立即计算 Attention
   - ✅ 移除独立的 Attention 定时任务
   - ✅ 统一后台任务调度

3. **开发工具**
   - ✅ 创建 check_status.sh（快速状态检查）
   - ✅ 创建 monitor_performance.py（性能监控）
   - ✅ 清理测试脚本和过时文档

4. **可视化与事件修复**
   - ✅ Attention 图改为显示合成注意力（包含 Google Trends/Twitter），历史段不再因新闻稀疏而接近 0
   - ✅ 事件标注使用合成注意力分位阈值，阈值比较由 ">=" 改为 ">"，并加入最小值过滤，显著减少 Spike/Wt 噪声
   - ✅ Dev Container 预装 GitHub Copilot 与 Copilot Chat 扩展（`.devcontainer/devcontainer.json`）

---

## 📋 推荐的进一步优化

### 性能优化
1. **添加缓存层**
   - 使用 Redis 缓存热点数据
   - 缓存 API 响应（特别是历史数据）
   - TTL: 价格数据 30秒，Attention 数据 1分钟

2. **数据库优化**
   - 添加复合索引：(symbol, datetime)
   - 分析慢查询
   - 考虑读写分离

3. **API 优化**
   - 启用 gzip 压应
   - 实现分页（大数据集）
   - 添加请求限流

### 前端优化
1. **数据加载**
   - 实现虚拟滚动（大数据集）
   - 懒加载图表
   - 预加载常用数据

2. **用户体验**
   - 添加加载动画
   - 实现数据缓存
   - 优化首屏加载时间

### 监控与告警
1. **日志优化**
   - 集成结构化日志
   - 添加性能追踪
   - 错误聚合和告警

2. **健康监控**
   - 添加 Prometheus metrics
   - 实现告警系统
   - 监控后台任务执行

---

## 🐛 故障排查

### 后端无法启动
```bash
# 检查端口占用
lsof -i :8000
kill -9 <PID>

# 检查依赖
pip install -r requirements.txt

# 查看详细错误
tail -50 logs/api.log
```

### 前端无法启动
```bash
# 检查端口占用
lsof -i :3000
kill -9 <PID>

# 重新安装依赖
cd web
rm -rf node_modules .next
npm install

# 查看详细错误
tail -50 logs/frontend.log
```

### 后台任务未运行
```bash
# 检查日志
grep "Scheduler\|Updater" logs/api.log | tail -20

# 重启服务
./scripts/stop_services.sh
./scripts/start_services.sh
```

---

## 📊 系统架构

```
┌─────────────────────────────────────────────────────────┐
│                    用户访问                              │
│              http://localhost:3000                      │
└────────────────────┬────────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────────┐
│              Next.js 前端 (3000)                         │
│   • React Components                                    │
│   • API Client (lib/api.ts)                            │
│   • Chart Components (lightweight-charts)              │
└────────────────────┬────────────────────────────────────┘
                     │ HTTP Requests
                     ▼
┌─────────────────────────────────────────────────────────┐
│            FastAPI 后端 (8000)                          │
│   • REST API Endpoints                                 │
│   • Background Tasks (asyncio)                         │
│   • Database ORM (SQLAlchemy)                          │
└────────────────────┬────────────────────────────────────┘
                     │
        ┌────────────┼────────────┐
        │            │            │
        ▼            ▼            ▼
┌──────────┐  ┌──────────┐  ┌──────────┐
│ SQLite   │  │ Binance  │  │ Google   │
│ Database │  │ API      │  │ Trends   │
└──────────┘  └──────────┘  └──────────┘
```

---

## 🎯 下一步

1. **访问前端界面**: http://localhost:3000
2. **浏览 API 文档**: http://localhost:8000/docs
3. **测试添加新代币**（前端操作）
4. **监控后台任务日志**: `tail -f logs/api.log`

---

**更新时间**: 2025-11-29  
**状态**: ✅ 系统运行正常
